#include "math.h"

namespace math {
double Add(double a, double b) { return add(a, b); }
double Sub(double a, double b) { return sub(a, b); }
double Div(double a, double b) { return div(a, b); }
}  // namespace math
